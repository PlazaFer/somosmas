import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles((theme) => ({
    containerSubtitle:{
        width: '100%',
        marginBottom: 50,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    }
}));